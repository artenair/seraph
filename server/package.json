{
  "name": "seraph",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,client,cloudflare -c cyan,magenta,yellow \"node --watch server.js\" \"npm run dev --prefix client\" \"cloudflared tunnel --config ~/.cloudflared/seraph-dashboard.yml run seraph-dashboard\"",
    "start": "node server.js",
    "import": "node scripts/import-json.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "firebase-admin": "^13.8.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
